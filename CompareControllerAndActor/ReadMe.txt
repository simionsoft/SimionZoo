This app compares the input of a continuous controller (PID, LQR, Vidal, Boukhezzar, Jonkman...) with its VFA-based approximation

The controller's output and the VFA's are randomly sampled over the input state-space

The app outputs a .txt file with the stats (min, max, avg, rms)
