<UserControl x:Class="Badger.Views.NeuralNetwork.LinkConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Badger.Views.NeuralNetwork.Parameters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical">
            <TextBlock Grid.Row="2" Text="Name:" Margin="0 0 0 0" Foreground="#222" FontSize="11" VerticalAlignment="Bottom"/>
            <TextBlock x:Name="TargetName" Margin="4 0 0 0"/>
            <TextBlock Grid.Row="2" Text="ID:" Margin="0 2 0 0" Foreground="#222" FontSize="11" VerticalAlignment="Bottom"/>
            <TextBlock x:Name="TargetID" Margin="4 0 0 0"/>
        </StackPanel>
        <Button Grid.Column="1" Style="{StaticResource DeleteButtonStyle}" Template="{StaticResource DeleteButtonTemplate}"/>
    </Grid>
</UserControl>
