<UserControl x:Class="Badger.Views.HerdAgentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Border Margin="0" BorderThickness="1">
        <StackPanel Orientation="Vertical" Margin="5">
            <StackPanel Orientation="Horizontal" Height="20" VerticalAlignment="Top" HorizontalAlignment="Center">
                <TextBlock IsEnabled="False" Width="Auto" Text="{Binding ipAddressString,FallbackValue=''}" Margin="0,0,5,0" />
                <TextBlock IsEnabled="False" Text="{Binding version,FallbackValue=''}" Foreground="Black" Margin="0,0,5,0" Width="Auto"/>
                <TextBlock IsEnabled="False" Text="{Binding status,FallbackValue=''}" Foreground="Black" Margin="0,0,5,0" Width="Auto"/>
                <TextBlock IsEnabled="False" Text="{Binding numProcessors,FallbackValue=''}" Foreground="Black" Margin="0,0,5,0" Width="Auto"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
