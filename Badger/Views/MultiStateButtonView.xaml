<UserControl x:Class="Badger.Views.MultiStateButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:Badger.ViewModels"
            xmlns:v="clr-namespace:Badger.Views"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Button x:Name="nextState" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" Margin="0">
        <Label FontFamily="Segoe UI Light" FontSize="8" x:Name="state" MinWidth="60">n/a</Label>
    </Button>
    
</UserControl>
