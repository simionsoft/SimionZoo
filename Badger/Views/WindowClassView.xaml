<UserControl x:Class="Badger.Views.WindowClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:Badger.ViewModels"
             xmlns:v="clr-namespace:Badger.Views"
             mc:Ignorable="d" 
             d:Height="300" d:Width="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.504,1.5" StartPoint="0.504,0.03">
                    <GradientStop Color="#fafafa" Offset="0"/>
                    <GradientStop Color="#fcfcfc" Offset="0.567"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <ContentControl x:Name="Class">

            </ContentControl>
            <Button Width="30" Height="30" Background="Transparent" cal:Message.Attach="[Event Click]=[Save()]" BorderBrush="{x:Null}" BorderThickness="0">
                <Image Source="/images/save.png" Width="25" Height="25" ></Image>
            </Button>
        </StackPanel>
    </ScrollViewer>
    
</UserControl>
