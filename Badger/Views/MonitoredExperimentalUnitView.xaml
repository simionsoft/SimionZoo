<UserControl x:Class="Badger.Views.MonitoredExperimentalUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             Background="Transparent" Width="190">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>


    <Border BorderThickness="1" BorderBrush="LightGray" ToolTip="{Binding ForksAsString}" MinHeight="20">
        <StackPanel Orientation="Horizontal">
            <Label Content="{Binding TaskName}" VerticalAlignment="Center" HorizontalContentAlignment="Right" Width="70" FontSize="10" Margin="2,0,2,0" Padding="0"/>
            <TextBlock Foreground="{Binding StateColor}" 
                        Text="{Binding StateString}" Background="Transparent"
                    FontSize="9" VerticalAlignment="Center" Margin="0" Width="50"/>
            <Button BorderThickness="0" Background="Transparent"  Margin="1" Width="16" Height="16" ToolTip="Show the experimental unit's log"
                        cal:Message.Attach="[Event Click]=[Action ShowLog()]">
                <Path Data="{StaticResource Notification}" Width="12" Height="12" Canvas.Left="17" Canvas.Top="10" Stretch="Uniform" Fill="#89000000" />

            </Button>
            
            <ProgressBar Margin="1,0,1,0" Width="40" Minimum="0" Maximum="100" Value="{Binding Progress}" Height="10"/>

        </StackPanel>
    </Border>
</UserControl>
