<UserControl x:Class="Badger.Views.MultiValuedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:Badger.ViewModels"
             xmlns:v="clr-namespace:Badger.Views"
             xmlns:help="clr-namespace:Badger.Views.Help"
             d:DesignWidth="300"
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type vm:IntegerViewModel}">
                <v:IntegerView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ClassViewModel}">
                <v:ClassView/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

        <StackPanel Orientation="Vertical">
        <Label Foreground="DarkGray" HorizontalAlignment="Right" VerticalContentAlignment="Center"
               HorizontalContentAlignment="Left" Width="150" 
               x:Name="Label" ToolTip="{Binding Comment}"/>
        <ItemsControl x:Name="AdedClasses"/>
            <ContentControl  x:Name="HeaderClass"/>
        </StackPanel>
       
</UserControl>
