<UserControl x:Class="Badger.Views.ShepherdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"  
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <!--<ListView x:Name="HerdAgentList" IsEnabled="False" IsManipulationEnabled="False" HorizontalContentAlignment="Center" 
              BorderBrush="{x:Null}" BorderThickness="0" ItemsSource="{Binding herdAgentList}" >
        <ListView.ItemTemplate>
            <DataTemplate>-->
    <ScrollViewer >
                <StackPanel Orientation="Vertical" Margin="0">
                    <StackPanel.Background>
                        <LinearGradientBrush EndPoint="0.504,1.5" StartPoint="0.504,0.03">
                            <GradientStop Color="#fafafa" Offset="0"/>
                            <GradientStop Color="#f0f0f0" Offset="1"/>
                        </LinearGradientBrush>
                    </StackPanel.Background>
                    <ItemsControl x:Name="herdAgentList"></ItemsControl>
                </StackPanel>
            <!--</DataTemplate>
        </ListView.ItemTemplate>
    </ListView>-->
    </ScrollViewer>
</UserControl>
