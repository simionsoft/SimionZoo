<UserControl x:Class="AppXML.Views.XMLNodeRefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:help="clr-namespace:AppXML.Views.Help"
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <help:ComboBoxToMaxItemWidthConverter x:Key="Convert"></help:ComboBoxToMaxItemWidthConverter>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">

        <Label Foreground="Gray" VerticalContentAlignment="Center"
                               HorizontalContentAlignment="Right" Width="150"  x:Name="Label" />
        <ComboBox x:Name="Options" SelectedItem="{Binding SelectedOption}" ItemsSource="{Binding Options}" Width="100"/>
    </StackPanel>
</UserControl>
