<Window x:Class="Badger.Views.ExperimentMonitorWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"  
        d:DesignHeight="600" d:DesignWidth="600"
        WindowStyle="ThreeDBorderWindow"
        Width="700"
        Height="800"
        WindowState="Normal"
        Title="Experiment Monitor"
        Icon="/badger-head.ico">

    <Grid Background="#FAFAFA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <oxy:PlotView Grid.Row="0" Grid.Column="0" Model="{Binding EvaluationPlot.Plot}" 
                      MinHeight="300" Margin="10,10,20,10" Background="#FAFAFA" />

        <ContentControl Grid.Row="1" Grid.Column="0" x:Name="ExperimentQueueMonitor" />
    </Grid>
</Window>
