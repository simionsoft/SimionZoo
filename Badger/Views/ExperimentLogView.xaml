<UserControl x:Class="AppXML.Views.ExperimentLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:AppXML.ViewModels"
            xmlns:v="clr-namespace:AppXML.Views"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <StackPanel Orientation="Vertical" >
        <Border BorderThickness="1" BorderBrush="LightGray" Margin="0">
       
            <StackPanel Margin="2">
                <StackPanel Margin="2" Orientation="Horizontal" HorizontalAlignment="Center" Width="180" >
                    <Label x:Name="ExperimentName" HorizontalAlignment="Center" BorderBrush="LightGray" Content="{Binding name}"  Width="150" FontFamily="Segoe UI" FontSize="10"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
