<UserControl x:Class="Badger.Views.MonitoredExperimentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:Badger.ViewModels"
            xmlns:v="clr-namespace:Badger.Views"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Border BorderThickness="1" BorderBrush="LightGray" Margin="0">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10,0" Width="578">
                <StackPanel Margin="2" Orientation="Horizontal">
                    <StackPanel Orientation="Vertical" Width="159">
                        <StackPanel>
                            <TextBlock x:Name="ExperimentName" IsEnabled="False" Text="{Binding name}" FontFamily="Segoe UI" FontSize="10"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Foreground="{Binding stateColor}" Text="{Binding stateString}"
                                           FontSize="8" Width="100"></TextBlock>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel>
                        <ScrollViewer VerticalScrollBarVisibility="Visible" CanContentScroll="True" Height="50" Width="415">
                            <TextBlock Text="{Binding statusInfo}" Width="397" FontSize="8" Foreground="Gray"/>
                        </ScrollViewer>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Visibility="{Binding Path=isRunning,Converter={StaticResource BoolToVis}}" Margin="10,0" Width="578">
                <ProgressBar Height="10" Width="380" Minimum="0" Maximum="100" x:Name="Progress" 
                                     Margin="0,0,0,0" Value="{Binding Path=progress}"/>
            </StackPanel>
        </StackPanel>
    </Border>

</UserControl>
