<UserControl x:Class="Badger.Views.PlotPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:Badger.ViewModels"
            xmlns:v="clr-namespace:Badger.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <StackPanel>
        <StackPanel Orientation="Vertical">

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox x:Name="bLegendVisible">Legend</CheckBox>
                <StackPanel Orientation="Vertical" IsEnabled="{Binding bLegendVisible}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="bLegendBorder">Border</CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label>Position</Label>
                        <ComboBox Text="Position" x:Name="legendOptions" Width="80"></ComboBox>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
           
        </StackPanel>
        
        <GroupBox Name="Visibility">
            <ContentControl x:Name="lineSeriesProperties"></ContentControl>
        </GroupBox>
    </StackPanel>
    

</UserControl>
