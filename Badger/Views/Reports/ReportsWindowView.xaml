<Window x:Class="Badger.Views.ReportsWindowView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:vm="clr-namespace:Badger.ViewModels"
            xmlns:cal="http://www.caliburnproject.org"
            xmlns:v="clr-namespace:Badger.Views"
            xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
            WindowStyle="ThreeDBorderWindow"
        Width="1024"
        Height="768"
        Background="#eeeeee"
        WindowState="Normal"
        Title="Report Viewer"
        Icon="/badger-head.ico">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <DataTemplate DataType="{x:Type vm:LoggedExperimentViewModel}">
            <v:LoggedExperimentView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoggedExperimentalUnitViewModel}">
            <v:LoggedExperimentalUnitView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoggedForkViewModel}">
            <v:LoggedForkView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoggedVariableViewModel}">
            <v:LoggedVariableView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlotViewModel}">
            <v:PlotView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatsViewModel}">
            <v:StatsView/>
        </DataTemplate>
        <BitmapImage x:Key="open-project" UriSource="/images/open-project.png" />
        <BitmapImage x:Key="plot" UriSource="/images/plot.png" />
        <BitmapImage x:Key="save-all" UriSource="/images/save-all.png" />
        <BitmapImage x:Key="spreadsheet" UriSource="/images/spreadsheet.png" />
        <BitmapImage x:Key="close" UriSource="/images/cancel.png"/>
        <BitmapImage x:Key="add" UriSource="/images/add.png"/>
    </Window.Resources>

    <StackPanel Orientation="Horizontal" Margin="2">
        <StackPanel Orientation="Vertical" Width="300" Margin="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button  HorizontalAlignment="Center" Width="40" Height="40" Margin="5"
                            x:Name="loadExperimentBatch" ToolTip="Load a batch: results from an experiment execution">
                    <StackPanel Orientation="Horizontal">
                        <!--
                        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0">Load</TextBlock>
                        -->
                        <Image Width="24" Height="24" Source="{StaticResource open-project}"/>
                    </StackPanel>
                </Button>
                <Button  HorizontalAlignment="Right" BorderBrush="SlateGray" Width="40" Height="40"
                                     cal:Message.Attach="[Event Click]=[Action saveReports()]"
                                    IsEnabled="{Binding Path=bCanSaveReports}">
                    <StackPanel Orientation="Horizontal">
                        <!--
                        <TextBlock VerticalAlignment="Center" Margin="3,0,0,0">Save all</TextBlock>
                        -->
                        <Image Width="24" Height="24" Source="{StaticResource save-all}"  Margin="3,0,3,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel Margin="2,10,2,2">
                <Label  Content="Experiment Forks" FontSize="14"
                       FontWeight="Medium" VerticalContentAlignment="Center" HorizontalAlignment="Center" />
                <TreeView x:Name="Experiments" Margin="0" Height="200" BorderBrush="DarkGray" BorderThickness="1" Background="White">
                    <ItemsControl x:Name="loggedExperiments"></ItemsControl>
                </TreeView>
            </StackPanel>
            <StackPanel Margin="2,10,2,2">
                <Label  Content="Variable Selection" FontSize="14"
                       FontWeight="Medium" VerticalContentAlignment="Center" HorizontalAlignment="Center" />
                <Border Margin="0" BorderBrush="DarkGray" BorderThickness="1">
                    <StackPanel Orientation="Vertical"  IsEnabled="{Binding bLogsLoaded}" Background="White">
                        <StackPanel Margin="0,2,0,2" Orientation="Horizontal" ToolTip="Which variables do we want to display?">
                            <CheckBox x:Name="bVariableSelection" VerticalAlignment="Center" Margin="4,0,0,0"></CheckBox>
                            <Label Width="67" Margin="0,0,0,0" VerticalAlignment="Center">Variables</Label>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                                <ItemsControl x:Name="variables" Width="190"/>
                            </ScrollViewer>
                        </StackPanel>
                        <StackPanel Margin="0,10,0,2" Orientation="Horizontal" ToolTip="Sets whether all variations of the experiments will be considered or just selected ones">
                            <Label Width="70" Margin="0,0,10,0">From</Label>
                            <ComboBox x:Name="fromOptions" SelectedValue="{Binding Path=selectedFrom}" Width="80" HorizontalAlignment="Center"></ComboBox>
                        </StackPanel>
                        <StackPanel Margin="0,2,0,2" Orientation="Horizontal" IsEnabled="{Binding bGroupsEnabled}" ToolTip="Forks currently used to group results">
                            <Label Width="70" Margin="0,0,10,0">Group by</Label>
                            <TextBlock Text="{Binding Path=groupBy}" MaxWidth="164" Width="164" Background="#eeeeee"></TextBlock>
                            <Button x:Name="resetGroupBy" Margin="4,0,0,0">
                                <Image Source="{StaticResource close}" Width="20"></Image>
                            </Button>
                        </StackPanel>
                        <StackPanel Margin="0,2,0,2" Orientation="Horizontal" IsEnabled="{Binding bGroupsEnabled}" ToolTip="How do we pick the best track from each group?">
                            <Label Width="70" Margin="0,0,10,0">Pick</Label>
                            <ComboBox x:Name="inGroupSelectionFunctions" SelectedValue="{Binding Path=selectedInGroupSelectionFunction}"
                                      Width="80" Margin="0,0,4,0" HorizontalAlignment="Center"></ComboBox>
                            <ComboBox x:Name="inGroupSelectionVariables" SelectedValue="{Binding Path=selectedInGroupSelectionVariable}"
                                      Width="80" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Margin="0,2,0,2" Orientation="Horizontal" ToolTip="This limits the maximum number of tracks to be displayed">
                            <Label Width="70" Margin="0,0,10,0">Limit to</Label>
                            <ComboBox x:Name="limitToOptions" SelectedValue="{Binding Path=selectedLimitToOption}"
                                      Width="80" Margin="0,0,10,0" HorizontalAlignment="Center"></ComboBox>
                        </StackPanel>
                        <StackPanel Margin="0,2,0,2" Orientation="Horizontal" IsEnabled="{Binding bIsOrderByEnabled}" ToolTip="Sets the criteria by which tracks are sorted before limiting">
                            <Label Width="70" Margin="0,0,10,0">Order by</Label>
                            <ComboBox x:Name="orderByFunctions" SelectedValue="{Binding Path=selectedOrderByFunction}"
                                      Width="80" Margin="0,0,4,0" HorizontalAlignment="Center"></ComboBox>
                            <ComboBox x:Name="orderByVariables" SelectedValue="{Binding Path=selectedOrderByVariable}"
                                      Width="80" HorizontalAlignment="Center"></ComboBox>
                        </StackPanel>

                        <StackPanel Margin="0,10,0,10" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="makeReport"  HorizontalAlignment="Center" BorderBrush="SlateGray"
                                    Margin="0,0" Height="30"
                                IsEnabled="{Binding Path=bCanGenerateReports}" >
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock VerticalAlignment="Center" Margin="2">Make Report</TextBlock>
                                    <!--
                                    <Image Source="{StaticResource plot}" Width="24" Margin="0"></Image>
                                    -->
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Vertical" Margin="2,2,2,2" Width="698">
            <StackPanel Margin="2,2,8,2" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <TabControl x:Name="reports" SelectedItem="{Binding selectedReport}" Margin="2,2,2,2" Height="708">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding name}" BorderBrush="{x:Null}"/>
                                <Button cal:Message.Attach="[Event Click]=[Action close()]" Background="{x:Null}" BorderBrush="{x:Null}">
                                    <Image Source="{StaticResource close}" Width="10"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                </TabControl>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Window>
