<UserControl x:Class="Badger.Views.PlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:Badger.ViewModels"
            xmlns:v="clr-namespace:Badger.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <BitmapImage UriSource="/images/settings.png" x:Key="settings"/>
        <BitmapImage UriSource="/images/save.png" x:Key="save"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
        <oxy:PlotView Model="{Binding Plot}" Width="580" Height="300"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Visibility="{Binding Path=bShowOptions,Converter={StaticResource BoolToVis}}">
            <Button Style="{StaticResource BadgerButtonStyle}" x:Name="showProperties" Width="25" Height="25">
                <Image Source="{StaticResource settings}" Width="22" Height="22"/>
            </Button>
            <Button Style="{StaticResource BadgerButtonStyle}" x:Name="saveImage" Width="25" Height="25" Margin="0,3,0,3">
                <Image Source="{StaticResource Save}" Width="22" Height="22"/>
            </Button>
        </StackPanel>
        
    </StackPanel>
</UserControl>
