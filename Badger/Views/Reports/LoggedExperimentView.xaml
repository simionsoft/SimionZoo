<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:Badger.Views"
             xmlns:vm="clr-namespace:Badger.ViewModels"
             xmlns:cal="http://www.caliburnproject.org" x:Class="Badger.Views.LoggedExperimentView"
             mc:Ignorable="d" 
                d:DesignHeight="50" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <TreeViewItem IsExpanded="True" Margin="0,0,0,0">
        <TreeViewItem.Header>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsChecked="{Binding bIsSelected}" VerticalAlignment="Center"
                          Visibility="{Binding Path=bCheckIsVisible,Converter={StaticResource BoolToVis}}"/>
                <Label x:Name="name" FontSize="10" FontFamily="Segoe UI Light" Content="{Binding name}"/>
            </StackPanel>
        </TreeViewItem.Header>

        <ItemsControl x:Name="forks" Margin="-19,0,0,0"/>
    </TreeViewItem>
</UserControl>
