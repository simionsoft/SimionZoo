<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:Badger.Views"
             xmlns:vm="clr-namespace:Badger.ViewModels"
             xmlns:cal="http://www.caliburnproject.org" x:Class="Badger.Views.LoggedExperimentView"
             mc:Ignorable="d" 
                d:DesignHeight="50" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,0" Background="#e0e0e0">
            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"
                          Visibility="{Binding Path=IsCheckVisible,Converter={StaticResource BoolToVis}}"
                          Margin="0"/>
            <Label x:Name="Name" FontWeight="Bold" Margin="0"/>
        </StackPanel>
        <StackPanel Margin="10,0,0,0">
            <ItemsControl x:Name="Forks" Padding="0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
