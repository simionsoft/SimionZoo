<UserControl x:Class="Badger.Views.PlotLineSeriesPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <StackPanel HorizontalAlignment="Left">
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="{Binding Path=bVisible,Mode=TwoWay}" VerticalAlignment="Center"/>
            <Label Content="{Binding Path=Name}" VerticalAlignment="Center"></Label>
        </StackPanel>
    </StackPanel>
</UserControl>
