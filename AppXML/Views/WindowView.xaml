<UserControl x:Class="AppXML.Views.WindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:AppXML.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:v="clr-namespace:AppXML.Views"
             mc:Ignorable="d" Height="580.597" Width="1324.291">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:BranchViewModel}">
            <v:BranchView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RightTreeViewModel}">
            <v:RightTreeView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>

        <TreeView  HorizontalAlignment="Left"  Height="410" Margin="23,48,0,0" VerticalAlignment="Top" Width="500"  Grid.ColumnSpan="2">
            <TreeViewItem x:Name ="Branches" Header="{Binding rootnode.name}"/>
        </TreeView>
        <Button Content="Load node" cal:Message.Attach="[Event Click]=[Action LoadSelectedNode()]" HorizontalAlignment="Left" Margin="591,143,0,0" VerticalAlignment="Top" Width="75" Visibility="{Binding RemoveChildVisible}"/>
        <Button Content="Save" cal:Message.Attach="[Event Click]=[Action Save()]" HorizontalAlignment="Left" Margin="591,268,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Load" HorizontalAlignment="Left" Margin="591,243,0,0" VerticalAlignment="Top" Width="75" cal:Message.Attach="[Event Click]=[Action Load()]" RenderTransformOrigin="0.607,2.05"/>
        <Button Content="Set as root" HorizontalAlignment="Left" Margin="591,218,0,0" VerticalAlignment="Top" Width="75" cal:Message.Attach="[Event Click]=[Action SetAsRoot()]" RenderTransformOrigin="0.607,3.6" />
        <StackPanel Margin="742,48,44,92">
            <ContentControl x:Name="Graf"/>
        </StackPanel>
        <Button Content="Add child" HorizontalAlignment="Left" Margin="591,193,0,0" VerticalAlignment="Top" Width="75" RenderTransformOrigin="0.113,1.475" cal:Message.Attach="[Event Click]=[Action AddChild()]" Visibility="{Binding AddChildVisible}"/>
        <Button Content="Remove child" HorizontalAlignment="Left" Margin="591,168,0,0" VerticalAlignment="Top" Width="75" Visibility="{Binding RemoveChildVisible}"  cal:Message.Attach="[Event Click]=[Action RemoveChild()]"/>
        <Button Content="Save all" HorizontalAlignment="Left" Margin="1080,506,0,0" VerticalAlignment="Top" Width="76" Visibility="{Binding RemoveChildVisible}"  cal:Message.Attach="[Event Click]=[Action SaveAll()]"/>
        <ComboBox x:Name="Apps" HorizontalAlignment="Left" Margin="87,10,0,0" VerticalAlignment="Top" Width="120" SelectedItem="{Binding SelectedApp}"/>
        <Label Content="APP" HorizontalAlignment="Left" Margin="23,10,0,0" VerticalAlignment="Top" RenderTransformOrigin="-1.381,0.758"/>

    </Grid>
</UserControl>
