<Window x:Class="AppXML.Views.WindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:AppXML.ViewModels"
             xmlns:v="clr-namespace:AppXML.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!--
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:BranchViewModel}">
            <v:BranchView/>
        </DataTemplate>
    </Window.Resources> -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="251*"/>
            <ColumnDefinition Width="266*"/>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="Tree" HorizontalAlignment="Left" Height="410" Margin="23,48,0,0" VerticalAlignment="Top" Width="352" Grid.ColumnSpan="2">
           
            <TreeViewItem ItemsSource="{Binding rootnode.children}" Header="{Binding rootnode.name}">
               
            </TreeViewItem>
        </TreeView>

    </Grid>
</Window>
