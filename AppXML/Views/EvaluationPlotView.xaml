<UserControl x:Class="AppXML.Views.EvaluationPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:vm="clr-namespace:AppXML.ViewModels"
            xmlns:v="clr-namespace:AppXML.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
            xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    
        <StackPanel HorizontalAlignment="Center">
            <!--<oxy:Plot x:Name="PlotView" Background="Yellow" Width="200" Height="200">
                <oxy:Plot.Series>
                    <oxy:LineSeries Title="Value" DataFieldX="xNorm"
                        DataFieldY="yValue"
                        ItemsSource="{Binding evaluationData}"
                        StrokeThickness="2" Color="Blue" />
                </oxy:Plot.Series>
            </oxy:Plot>-->
            <oxy:PlotView Model="{Binding evaluationPlot}" Width="580" Height="300"/>
    

    </StackPanel>
</UserControl>
