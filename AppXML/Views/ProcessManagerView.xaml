<UserControl x:Class="AppXML.Views.ProcessManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:vm="clr-namespace:AppXML.ViewModels"
             xmlns:v="clr-namespace:AppXML.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type vm:ProcessStateViewModel}">
                <v:ProcessStateView/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    
        <StackPanel Margin="20">
            <ItemsControl x:Name="Processes"></ItemsControl>
        </StackPanel>
    
</UserControl>
