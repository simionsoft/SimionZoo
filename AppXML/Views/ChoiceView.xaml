<UserControl x:Class="AppXML.Views.ChoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:AppXML.ViewModels"
             xmlns:v="clr-namespace:AppXML.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type vm:ClassViewModel}">
                <v:ClassView/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Label x:Name="Label" Width="100"></Label>
            <ComboBox SelectedIndex="0" ItemsSource="{Binding Combo}" Width="150" cal:Message.Attach="[Event SelectionChanged]=[Action ComboChanged($this)]">  
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <ContentControl>
                <Label x:Name="Class" Width="150"></Label>
            </ContentControl>
            
        </StackPanel>
    </StackPanel>
</UserControl>
