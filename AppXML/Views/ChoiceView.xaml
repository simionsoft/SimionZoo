<UserControl x:Class="AppXML.Views.ChoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:AppXML.ViewModels"
             xmlns:v="clr-namespace:AppXML.Views"
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type vm:ClassViewModel}">
                <v:ClassView/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical" Margin="-15,0,15,0">
        <StackPanel Orientation="Horizontal">
           <!-- <Label x:Name="Label" Width="200"></Label>-->
            <ComboBox Width="200" ItemsSource="{Binding Combo}"  SelectedItem="{Binding SelectedItem}">  
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <ContentControl x:Name="Class" Margin="10,0,0,0">
            </ContentControl>
        </StackPanel>
    </StackPanel>
</UserControl>
